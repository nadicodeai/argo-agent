# argo-agent

The argo agent. Deployable into customer environments.

> Scaffolding placeholder. Full README lands in M6 after the rename is stable.

## Quick start

```sh
pip install -e '.[dev]'
argo --help
```

## Stay in sync

```sh
argo update
```

See `argo update --help` for the full sync model.
