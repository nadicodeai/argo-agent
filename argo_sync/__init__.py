"""argo_sync — rename engine for argo-agent.

Provides :class:`~argo_sync.engine.RenameEngine` (the ordered rename pipeline)
and :class:`~argo_sync.config.RenameConfig` (the declarative YAML config loader).
This package is stdlib + pyyaml only; no further runtime dependencies.
"""

from __future__ import annotations

__version__ = "0.0.1"
